// The keys and values are the same string for simplicity and easy debugging
// It's easier to find and fix typos in variable names (keys) than in string values
const PlayerActionTypes = {
  FETCH_ALL_PLAYERS: "FETCH_ALL_PLAYERS",
};

// Export the action types object as the default export
// This allows it to be imported in other files with a single line
export default PlayerActionTypes;
